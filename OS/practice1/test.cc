#include <iostream>
#include <chrono>
#include <string>
int main()
{
  // std::cout << std::chrono::system_clock::now() << std::endl;

  auto start = std::chrono::steady_clock::now();



  return 0;
}
